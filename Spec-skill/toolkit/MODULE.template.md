# [模块名] 模块规范

> 仅为**复杂/安全敏感**模块编写此文档。简单模块只需在 CONTEXT.md 定义术语。

**最后更新：** [日期]
**对应代码：** [路径，如 `src/auth/`]

---

## 1. 职责
[这个模块负责什么，边界在哪]

## 2. 核心流程
[关键流程，分步骤；必要时画 ASCII 流程图]

```
请求 → 校验 → 处理 → 持久化 → 返回
```

## 3. 接口定义

### POST /api/v1/xxx
- **输入：** `{ field: type }`
- **输出：** `{ field: type }`
- **错误：** 400 / 401 / 409 ...

## 4. 数据模型
[实体字段、约束、关系]

## 5. 约束与安全要求
- [必须遵守的规则，如：密码用 bcrypt rounds=12]
- [幂等性、签名校验、不记录敏感日志等]

## 6. 实现检查清单
- [ ] [关键实现点 1]
- [ ] [关键实现点 2]
- [ ] 单元测试覆盖核心流程
